# ملاحظات تدقيق SEO لمنصة الناصر

أظهر التحقق من الرابط المنشور `https://alnaseer.org/robots.txt` في 19 أغسطس 2026 أن الطلب يصل حاليًا إلى واجهة التطبيق أحادية الصفحة ويعرض صفحة 404 بدلاً من ملف نصي خاص بالزواحف. لذلك، على الرغم من وجود `public/robots.txt` داخل المستودع، فإن قواعد النشر تحتاج إلى تعديل صريح ليستثني ملف robots من إعادة الكتابة العامة ويقدمه بمسار ثابت صحيح.

يوجد مولد خريطة موقع ديناميكي عبر `/api/sitemap`، وتعلن قواعد النشر إعادة كتابة `/sitemap.xml` إليه. ستُضاف صفحة البوت إلى المولد، كما سيُراجع تقديم ملف robots ليعلن رابط الخريطة المنشور بصورة قابلة للوصول.

تنتج طبقة العرض المسبق في `api/render.ts` بيانات meta وJSON-LD للزواحف. يلزم إضافة مسار مستقل لصفحة البوت في هذه الطبقة وفي واجهة React، حتى يرى Google محتوىً متسقًا وقابلًا للزحف بدل الاكتفاء بقالب SPA عام.

## مرجع فهرسة Google

توضح [إرشادات خرائط المواقع من Google](https://developers.google.com/search/docs/crawling-indexing/sitemaps/overview) أن الخريطة تساعد الزواحف في اكتشاف الروابط المهمة، لكنها لا تضمن زحف كل رابط أو فهرسته. وتؤكد [Google Search Essentials](https://developers.google.com/search/docs/essentials) ضرورة المحتوى المفيد الموجه للإنسان، والعناوين والوصلات القابلة للزحف. كما توصي [إرشادات البيانات المنظمة](https://developers.google.com/search/docs/appearance/structured-data/intro-structured-data) باستخدام JSON-LD مكتملًا ودقيقًا ومطابقًا للمحتوى المرئي، مع اختباره بعد النشر. وتوضح [إرشادات robots meta](https://developers.google.com/search/docs/crawling-indexing/robots-meta-tag) أن تعليمات الفهرسة لا تُقرأ إذا حُجب الرابط عن الزحف في robots.txt.

## بيانات المنصة العامة

يوجد في مشروع Supabase المرتبط بالمنصة 5 مستويات، و61 مادة، و27,355 سؤالًا نشطًا في جداول الاختبارات، إلى جانب 1,997 قاعدة قضائية و344 وثيقة قانونية. ستستخدم صفحة البوت هذه الأعداد على نحو عام ومتزن، وستختار خريطة الموقع صفحات اختبارات وأسئلة عامة فقط من المستويات الأربعة، من دون إدراج حسابات أو نتائج أو صفحات تتطلب تسجيل دخول.

## التحقق البصري

تمت معاينة صفحة `/bot` محليًا بعد إضافة إعداد المضيف المسموح في Vite. ظهرت الصفحة باللغة العربية وبعنوان رئيسي واحد، وفقرات وصفية واضحة، وروابط مباشرة إلى البوت وصفحات الاختبارات، وأقسام مرتبة للخدمات وطريقة الاستخدام. كما ظهر رابط الصفحة الجديد ضمن التنقل الداخلي، وهو عنصر يساعد الزواحف على اكتشافه بجانب إدراجه في خريطة الموقع.
